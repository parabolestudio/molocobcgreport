import * as React from "react";
import * as Types from "./types";

declare function AboutHero(props: {
  as?: React.ElementType;
  buttonBlueMediumWatch?: React.ReactNode;
  textWhiteText?: React.ReactNode;
  textWhiteText2?: React.ReactNode;
}): React.JSX.Element;
