import * as React from "react";
import * as Types from "./types";

declare function ProgramDetailHero(props: {
  as?: React.ElementType;
  headingText?: React.ReactNode;
  subtext?: React.ReactNode;
}): React.JSX.Element;
